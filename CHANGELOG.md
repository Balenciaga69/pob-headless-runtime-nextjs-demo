# Changelog

## Unreleased

- added stable API support for `list_items`, `list_skills`, `select_skill`, and `get_selected_skill`
- added `/api/items`
- added `/api/build/skills`, `/api/build/skills/select`, and `/api/build/skills/selected`
- updated the dashboard with skills and items panels
- updated the stable contract snapshot and client types
